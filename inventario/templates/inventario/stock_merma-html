{% extends 'base.html' %}

{% block content %}
<div class="row mb-4">
    <div class="col">
        <h1 class="display-5">Registro de Merma</h1>
        <p class="lead">Registra pérdidas de productos en el inventario</p>
    </div>
    <div class="col-auto">
        <a href="{% url 'stock_admin' %}" class="btn btn-outline-secondary">
            Volver al Inventario
        </a>
    </div>
</div>

<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="card shadow-sm">
            <div class="card-header">
                <h5 class="mb-0">Formulario de Merma</h5>
            </div>
            <div class="card-body">
                <form method="post">
                    {% csrf_token %}
                    
                    <div class="mb-3">
                        <label for="{{ form.producto.id_for_label }}" class="form-label">Producto</label>
                        {{ form.producto }}
                        {% if form.producto.errors %}
                            <div class="invalid-feedback d-block">
                                {{ form.producto.errors|first }}
                            </div>
                        {% endif %}
                    </div>
                    
                    <div class="mb-3">
                        <label for="{{ form.cantidad.id_for_label }}" class="form-label">Cantidad</label>
                        <div class="input-group">
                            {{ form.cantidad }}
                            <span class="input-group-text" id="unidad-display">-</span>
                        </div>
                        {% if form.cantidad.errors %}
                            <div class="invalid-feedback d-block">
                                {{ form.cantidad.errors|first }}
                            </div>
                        {% endif %}
                    </div>
                    
                    <div class="mb-3">
                        <label for="{{ form.motivo.id_for_label }}" class="form-label">Motivo</label>
                        {{ form.motivo }}
                        {% if form.motivo.errors %}
                            <div class="invalid-feedback d-block">
                                {{ form.motivo.errors|first }}
                            </div>
                        {% endif %}
                    </div>
                    
                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-warning">Registrar Merma</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

{% block scripts %}
<script>
document.addEventListener('DOMContentLoaded', function() {
    const productoSelect = document.getElementById('{{ form.producto.id_for_label }}');
    const unidadDisplay = document.getElementById('unidad-display');
    
    // Actualizar unidad cuando cambia el producto
    productoSelect.addEventListener('change', function() {
        const selectedOption = this.options[this.selectedIndex];
        const unidad = selectedOption.getAttribute('data-unidad');
        unidadDisplay.textContent = unidad || '-';
    });
    
    // Inicializar con el valor por defecto
    if (productoSelect.selectedIndex >= 0) {
        const selectedOption = productoSelect.options[productoSelect.selectedIndex];
        const unidad = selectedOption.getAttribute('data-unidad');
        unidadDisplay.textContent = unidad || '-';
    }
});
</script>
{% endblock %}
{% endblock %}